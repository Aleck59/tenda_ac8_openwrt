# Official OpenWrt release whose mipsel_24kc packages the images install
# from (same libc, libubox, libubus, libuci and mbedtls versions as the
# pinned tree).  Kernel modules come from the feed of each build.
PACKAGES_RELEASE=24.10.8
